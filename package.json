{
  "name": "pauta",
  "version": "1.0.0",
  "private": true,
  "description": "Pauta — self-contained native Android build of the Pauta web app.",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "inject:native": "node scripts/inject-native.mjs",
    "sync": "npm run build:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
