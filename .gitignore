node_modules/
www/
# Anchored to the repo root so the local plugin's focus-activity/android/ source
# stays tracked (a bare "android/" would ignore it too).
/android/
resources/
# Mas o ícone-fonte para o Android APK é gerado por gen-icons.mjs e deve
# ser versionado para que `npx @capacitor/assets generate` funcione após clone.
!resources/icon.png
*.apk
